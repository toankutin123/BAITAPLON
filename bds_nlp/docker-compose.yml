version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: bds_nlp_postgres
    environment:
      POSTGRES_DB: bds_nlp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
    volumes:
      - postgres_bds_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_bds_data:
